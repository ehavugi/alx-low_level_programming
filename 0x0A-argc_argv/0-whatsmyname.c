#include <stdio.h>
/**
 * main - main function
 * @argc: number of arguments
 * @argv: array
 *
 * Return: Always 0.
 */

int main(int argc, char *argv[])
{
	if (argc > 0)
	{
		printf("%s\n", argv[0]);
	}
	return (0);
}
