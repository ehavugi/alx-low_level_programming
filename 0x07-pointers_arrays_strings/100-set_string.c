/**
 * set_string -  set the value of a pointer to a char
 * @s: input pointer to a string
 * @to: input to be used pointer to hold the string.
 *
 * Return: void
 */
void set_string(char **s, char *to)
{
	*s = to;
}
